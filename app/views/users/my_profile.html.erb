<%=render "shared/profile" ,
  profilePhoto: @user.photo.key,
  userFirstName: @user.first_name.titleize,
  userLastName: @user.last_name.titleize,
  userLocation: @user.location.titleize,
  skills:@specialties,
  user: @user.about,
  experiences: @experiences,
  education: @education
%>
